<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>dot_net_course</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- GUI tasks are separate projects; do not compile their folders into this app. -->
  <ItemGroup>
    <Compile Remove="Task5/**" />
  </ItemGroup>

</Project>
